<!--
==================================================
Header Section Start
================================================== -->
<header id="top-bar" class="navbar-fixed-top animated-header">
    <div class="container">
        <div class="navbar-header">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
            <!-- responsive nav button -->
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            </button>
            <!-- /responsive nav button -->
            {% endif %}

            <!-- logo -->
            <div class="navbar-brand">
                <a href="{{ path('oc_platform_app') }}" >
                    <img src="{{ asset("images/lfaj-logo2.png") }}" alt="">
                </a>
            </div>
            <!-- /logo -->
        </div>
        <!-- main menu -->
        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
        <nav class="collapse navbar-collapse navbar-right" role="navigation">
            <div class="main-menu">
                <ul class="nav navbar-nav navbar-right">
                    <li>
                        <a href="{{ path("oc_platform_homepage") }}" >View Offers</a>
                    </li>
                    <li><a href="{{ path("oc_platform_add") }}">Post an Offer</a></li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">My Account<span class="caret"></span></a>
                        <div class="dropdown-menu">
                            <ul>
                                <li><a href="{{ path("fos_user_profile_show") }}">Profile</a></li>
                                <li><a href="{{ path("fos_user_security_logout") }}">Disconnect</a></li>
                            </ul>
                        </div>
                    </li>
                </ul>
            </div>
        </nav>
        {% endif %}
        <!-- /main nav -->
    </div>
</header>
